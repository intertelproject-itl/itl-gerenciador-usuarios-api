services:
  itl-gerenciador-usuarios-api:
    container_name: itl-gerenciador-usuarios-api
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "5000:8080"
    environment:
      ASPNETCORE_ENVIRONMENT: Development
      ASPNETCORE_URLS: http://+:8080
    restart: always